{
  "homepage": "https://github.com/dhatchu97/flask-auth.git",
  "dependencies": {
    "gh-pages": "^6.2.0"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
